# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: cfin.custom.zblocklistgovern
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: CF2_100_Manik
          url: http://gl-cf2-aem-d.abb.com:443
          client: '100'
        app:
          name: ZBLOCKLISTGOV
          package: ZCFIN_FIORI
          transport: CF2K958646
        exclude:
          - /test/
          - /localService/
